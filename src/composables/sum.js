export default function () {
    const sum = (
        ... numbers 
    ) => 
        numbers.reduce((acc, n) => acc + n, 0);
        
         
    

    return { sum }
    
    // const sum = (a, b) => {
    //     return a + b;
    // }

    // return { 
    //     sum
    // };

}

